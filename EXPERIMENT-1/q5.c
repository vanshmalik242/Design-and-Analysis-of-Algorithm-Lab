//Calculate the remainder, and the quotient of two given numbers.

#include <stdio.h>

int main() {
    int a, b;
    scanf("%d %d", &a, &b);
    printf("Quotient: %d, Remainder: %d\n", a / b, a % b);
    return 0;
}
